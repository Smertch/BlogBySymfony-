{% extends 'layouts/auth.html.twig' %}

{% block content %}
    <div class="form-wrapper">
        <div class="form-title">
            <i class="fas fa-shield-alt"></i> {{ ui('twofa.title') }}
        </div>
        <p class="text-muted small mb-4">
            {{ ui('twofa.intro') }}
        </p>

        <form action="{{ path('2fa_login_check') }}" method="post">
            <input type="hidden" name="_csrf_token" value="{{ csrf_token('two_factor_authenticate') }}">

            <div class="mb-4 input-icon-wrapper">
                <i class="fas fa-key"></i>
                <input type="text" name="_auth_code" inputmode="numeric" pattern="[0-9]*" maxlength="6" autocomplete="one-time-code" class="form-control" placeholder="{{ ui('twofa.placeholder') }}" required>
            </div>

            <button type="submit" class="btn-submit">{{ ui('twofa.verify') }} <i class="fas fa-arrow-right ms-2"></i></button>
        </form>

        <div class="extra-links d-flex justify-content-between mt-4">
            <a href="{{ path('logout') }}">{{ ui('twofa.cancel') }}</a>
        </div>
    </div>
{% endblock %}
